import type {CandidateResult,Result} from './elections';
export default function CandidateProfile({result,selected}:{result:Result;selected:CandidateResult[]}){return selected.length?<div className="ea-profile-grid">{selected.map(c=><section className="ea-source-card" key={c.id}><span className="ea-eyebrow">{result.event.office} · {result.event.year}</span><h2>{c.name}</h2><p>{c.fullName}</p><dl>{[['Número',c.number],['Partido',c.party],['Situação na eleição',c.status.startsWith('#')?'Não informada':c.status],['Idade na eleição',c.profile?.ageAtElection!=null?`${c.profile.ageAtElection} anos`:null],['Escolaridade',c.profile?.education],['Ocupação',c.profile?.occupation],['Gênero declarado',c.profile?.gender],['Cor/raça declarada',c.profile?.race],['UF de nascimento',c.profile?.birthState],['Situação da candidatura',c.profile?.registration],['Federação',c.profile?.federation],['Coligação',c.profile?.coalition],['Composição',c.profile?.coalitionParties]].map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value||'Não disponível no arquivo'}</dd></div>)}</dl><p className="ea-search-note">Informações declaradas para a eleição de {result.event.year}. A situação é histórica, não confirma o exercício atual de um mandato. <a href={`https://dadosabertos.tse.jus.br/dataset/candidatos-${result.event.year}`} target="_blank" rel="noopener noreferrer">Fonte: cadastro de candidaturas do TSE</a>{c.profile&&` · Arquivo gerado em ${c.profile.generatedAt}`}.</p></section>)}</div>:<div className="ea-empty"><h2>Escolha um candidato na aba Votação.</h2></div>;}
